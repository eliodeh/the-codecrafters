# A detailed summary operators
in golang operators to perform operations on variables and values, it also have + operator that adds two values together.
example:
func main() {
    var k = 15 + 25
    fmt.println(k)
}
+ operator can also add variable and value.
example:
func main() {
    var (
        num = 2 + 2
    )
}
 