# A detailed summary of Arrays
A Go Arrays handles multiple value storage of the kind within a single variable. arrays are declared in two ways which are 1. variable of keyword and
2. variable with sign (:=).
# example of Arrays:
func main() {
    var arr1 = [2] int {1, 2}

    fmt.Println(arr1)
}

