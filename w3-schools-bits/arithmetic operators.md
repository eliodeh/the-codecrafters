# A detailed summary of arithmetic operators
Arithmetic operators in Go are used to function with maths operations.

# my illustrations:
the maths functions includes addition(+), substration(-), multiplication(*),division(/), increament(++), decreament(--) and modulus(%).
