# A detailed summary of Data types
A Go data types handles the size and type of values. data types consist of bool, numeric and string for handling values

# my illustration
func main() {
    var x bool = true
    var y string = "Hello"
}
. Integer data types handles values of whole numbers that don't have decimals. 
 it also have categories, which are 1 signed integers and 2 unsigned integers and its default type is (int).